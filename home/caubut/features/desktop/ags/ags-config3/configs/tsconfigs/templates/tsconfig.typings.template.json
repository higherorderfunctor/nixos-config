{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": ["../../../tsconfig.base.json"],
  "include": ["${configDir}/index.d.ts"],
  "compilerOptions": {
    "noEmit": true,
    "rootDir": "${configDir}",
    "tsBuildInfoFile": "${configDir}/.tsbuildinfo/typings.tsbuildinfo"
  }
}
