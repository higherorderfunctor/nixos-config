name: "dungeon-crawler-test-harness"
description: "Procedural Myst-style dungeon crawler validating OPA→RAG→LangGraph at scale"
trigger_type: "agent"
blocks:
  - id: "navigate"
    name: "navigate"
    description: "Move to adjacent room, validate exit, retrieve tier 1 surface facts. Track visited rooms."
    execution_env: "inline"
  - id: "assess"
    name: "assess"
    description: "AI decision hub — evaluate room facts, inventory, history, choose next action."
    execution_env: "inline"
  - id: "inspect"
    name: "inspect"
    description: "Deep examination of object/feature. Triggers tier 2 fact discovery. OPA-scoped."
    execution_env: "inline"
  - id: "pickup"
    name: "pickup"
    description: "Acquire item from room. Validate obtainability, check constraints, retrieve tooltip."
    execution_env: "inline"
  - id: "combine"
    name: "combine"
    description: "Merge two inventory items via discovered recipe. Consume inputs, produce result."
    execution_env: "inline"
  - id: "use_item"
    name: "use_item"
    description: "Apply item to room object. Trigger state changes, unlock paths, retrieve tier 3 facts."
    execution_env: "inline"
  - id: "verify"
    name: "verify"
    description: "Terminal — compare AI solution against deterministic solver. Compute all metrics."
    execution_env: "inline"
