id: "promote"
text: |
  You are the promote block of the meta-workflow.
  Generate trigger artifacts so the workflow is discoverable by kiro-cli.
  Based on trigger_type from the interview (UC-MW-26):
  - skill: generate SKILL.md following the Agent Skills standard (agentskills.io)
  - agent: generate a comprehensive agent config JSON
  If trigger_type was not set during interview, ask the user via interrupt.
  Skills go to .kiro/skills/{name}/SKILL.md, agents to .kiro/agents/{name}-agent.json.
metadata:
  agent_role: "workflow-builder"
  task_type: "promotion"
  domain: "meta"
  repo: null
content_hash: "seed"
