{
  "name": "dungeon-crawler-test-harness",
  "description": "Procedural Myst-style dungeon crawler validating OPA→RAG→LangGraph at scale (100K+ facts)",
  "model": "auto",
  "prompt": "You are the dungeon-crawler-test-harness agent.\n\nProcedural Myst-style dungeon crawler. Deterministic generator creates rooms, items, puzzles, solvable path. AI agent navigates via OPA-scoped RAG facts. Deterministic verifier compares AI actions against optimal solver path. Validates full OPA→RAG→LangGraph stack at scale.\n\n## Blocks\n\n- navigate: Move player between rooms using tier 1 RAG (room connections, basic descriptions)\n- assess: AI decision-making — evaluate room state, inventory, choose next action (subagent)\n- inspect: Examine object/feature using tier 2 RAG (detailed descriptions, hidden properties, puzzle hints)\n- pickup: Take item from room into inventory\n- combine: Merge two inventory items per dungeon rules\n- use_item: Apply inventory item to room feature/puzzle using tier 3 RAG (solutions, interactions, state transitions)\n- verify: Compare AI action sequence against deterministic optimal solver path\n\n## Status\n\nPhase 6 — workflow artifacts generated by meta-workflow. TypeScript implementation pending (6.5).",
  "tools": ["*"],
  "allowedTools": [
    "@kiro-cortex",
    "openmemory_query",
    "openmemory_store",
    "sequentialthinking",
    "fs_read",
    "fs_write",
    "code",
    "grep",
    "glob",
    "execute_bash"
  ],
  "includeMcpJson": true,
  "resources": [
    "file:///home/caubut/Documents/projects/nixos-config/home/caubut/features/cli/code/kiro/kiro-cortex/resume.md"
  ],
  "hooks": {
    "agentSpawn": [
      {
        "command": "cd ~/Documents/projects/nixos-config/home/caubut/features/cli/code/kiro/kiro-cortex && echo '=== kiro-cortex status ===' && git log --oneline -5",
        "timeout_ms": 5000
      }
    ]
  },
  "welcomeMessage": "Dungeon crawler test harness agent ready. Workflow artifacts exist — TypeScript implementation is next (Phase 6.5)."
}
