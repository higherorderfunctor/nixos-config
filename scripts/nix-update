#!/usr/bin/env bash

set -euETo pipefail
shopt -s inherit_errexit 2>/dev/null || :

cd "$(dirname "$0")"/..

nvfetcher -k "/run/user/$(id -u)/secrets/${USER}-nvfetcher-keyfile" -o overlays/.nvfetcher

nix flake update
