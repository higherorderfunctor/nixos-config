# nixos-config

Note: manually update /etc/nix/nix.conf with trusted-users
<https://github.com/NixOS/nix/issues/8248>

New way to switch??

```
home-manager --show-trace switch -b backup --flake .
```
